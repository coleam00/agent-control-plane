{
  "name": "agent-control-plane-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "migrate": "bun run src/migrate.ts",
    "smoke": "bun run src/smoke.ts",
    "itest": "bun run src/itest.ts",
    "test": "bun test src"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  }
}
